const request = require('supertest');



// module.exports = function (app) {
//     it('GET /api/test 성공 시 Status Code는 1000 을 반환한다.', async () => {
    
//         // 명시한 api 경로를 통해 요청한 후 값을 받아온다.
//         const response = await request(app).get('/app/test')
//         console.dir(response);
//         // 응답한 값이 예상한 값과 맞는 지 비교한다.
//         expect(response.statusCode).toBe(1000);
//     });   
// }  


    
    


